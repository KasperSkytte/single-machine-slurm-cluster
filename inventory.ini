[slurm_allinone]
localhost ansible_connection=local

# To run this against a different / remote machine instead, replace the
# line above with something like:
# myhost ansible_host=192.168.1.50 ansible_user=youruser
